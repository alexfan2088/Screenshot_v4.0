<Window x:Class="Screenshot_v3_0.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="智能截屏 v3.0" Height="400" Width="500">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    
    <Grid Margin="16" Grid.Row="0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- 菜单栏 -->
      <Menu Grid.Row="0" Margin="0,0,0,10">
        <MenuItem Header="设置">
          <MenuItem Header="录制设置..." Click="MenuSettings_Click"/>
        </MenuItem>
      </Menu>

      <!-- 视频录制 -->
      <TextBlock Text="视频录制" FontWeight="Bold" Margin="0,0,0,8" Grid.Row="1"/>
      <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,16">
        <Button Content="开始录制视频" Width="140" Margin="0,0,12,0" 
                Click="BtnStartVideo_Click" x:Name="BtnStartVideo"/>
        <Button Content="停止录制视频" Width="140" 
                Click="BtnStopVideo_Click" x:Name="BtnStopVideo" IsEnabled="False"/>
      </StackPanel>
    </Grid>
    
    <!-- 状态栏 -->
    <StatusBar Grid.Row="1" Background="#F0F0F0" BorderBrush="#C0C0C0" BorderThickness="0,1,0,0">
      <StatusBar.ItemsPanel>
        <ItemsPanelTemplate>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
          </Grid>
        </ItemsPanelTemplate>
      </StatusBar.ItemsPanel>
      <StatusBarItem x:Name="StatusBarConfigItem" Grid.Column="0">
        <TextBlock x:Name="StatusBarConfig" Text="视频：100%分辨率，60fps，H.264 | 音频：44.1kHz，192kbps" 
                   Margin="5,0" VerticalAlignment="Center"/>
      </StatusBarItem>
      <Separator Grid.Column="1"/>
      <StatusBarItem x:Name="StatusBarInfoItem" Grid.Column="1">
        <TextBlock x:Name="StatusBarInfo" Text="就绪" Margin="5,0" VerticalAlignment="Center"/>
      </StatusBarItem>
    </StatusBar>
  </Grid>
</Window>
